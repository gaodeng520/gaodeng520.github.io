name: Build C Code

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Install GCC
      run: sudo apt-get update -y && sudo apt-get install -y gcc

    - name: Build C code
      run: gcc -o test.exe main.c
